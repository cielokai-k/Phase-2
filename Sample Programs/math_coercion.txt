activate {

    pulse a = 10;
    spark b = 2.5;
    stream c = 3.14159;

    express("=== MATH OPERATIONS ===");

    express(a + 5);
    express(a + b);
    express(c * 2);
    express(a / 2);
    express(a % 3);
    express(a ** 2);

    express("=== TYPE COERCION ===");

    pulse whole;
    whole = 8.9;

    express(whole);

    synapse flag;
    flag = 1;

    express(flag);

    thought text = "Hello";

    express(text + " World");

}