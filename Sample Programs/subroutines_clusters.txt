subroutine add(pulse a, pulse b) {
    recall a + b;
}

subroutine displayMessage(thought msg) {
    express(msg);
}

cluster pulse numbers = [1, 2, 3, 4, 5];

express("=== SUBROUTINES ===");

pulse result = add(10, 20);
express(result);

displayMessage("Hello from subroutine");

express("=== CLUSTERS ===");

express(numbers[0]);
express(numbers[2]);
express(numbers[4]);